# Local configuration — copy to config.local.env and edit.
# config.local.env is gitignored.

export FS_LICENSE=/path/to/freesurfer/license.txt
export SUBJECTS_DIR="${SUBJECTS_DIR:-$PWD/freesurfer_subjects}"

# Optional: override default paths
# export SUBJECTS_TSV="$PWD/subjects.tsv"
# export BIDS_ROOT="$PWD/Data_ASL"
# export ASL_DICOM_ROOT=/path/to/dicom/mount
